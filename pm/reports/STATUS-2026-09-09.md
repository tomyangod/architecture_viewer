# 项目现状全览 · 2026-09-09

> 对外可用快照。数字以本文件与 [`pm/metrics.md`](../metrics.md) 为准；WBS 看板见 [`pm/README.md`](../README.md)。  
> 本轮焦点：**架构审核评估 → 真缺口修复** + **npm `0.11.2` 已上线**。

## 一句话

**`arch-viewer@0.11.2` 已在 npmjs `latest`**；P0 会话门 + 审核中的真实缺口（配置化 / 错误分级 / AI 容错 / Schema 迁移 / 扫描白名单）已落地。工作区仍有大量未提交 WIP；审核修复**尚未 commit**。变现与扩展上架仍未收口。

## 本轮回顾（架构审核）

对照「分析器 / 规则 / AI / MCP / 测试」审核稿逐条核验后：

| 结论 | 含义 |
|---|---|
| **真缺口已修** | Analyzer 契约补 `generateConfigSuggestion`；`analyzer-error` 按 `errorType` 分级；规则阈值/开关/排除进 `architecture-rules.yaml`；LLM 超时+指数退避+备用端点+分场景模型；盲评加权 winner；报告 v1→v2 迁移；finding 按 severity 筛；密钥文件跳过 + `AV_SCAN_ALLOW` |
| **审核误判 / 已有能力** | AI 路径后置校验（`verifyPaths`）、`removed-type` 分级依据、Java/Go 内置提取、CI 门禁、MCP「单仓给结论」设计 |
| **刻意不做** | builtin 只重算变更子图（会漏环）；规则拓扑排序；`av_batch_scan`；插件生态 / 历史快照 / 大仓压测（另开产品线） |

相关代码入口：`lib/llm-client.js`、`lib/analyzers/{analyzer,external,index}.js`、`lib/rules.js`、`lib/risk-rules.js`、`lib/report-contract.js`、`lib/extract/shared.js`、`lib/orch/blind-score.js`。

## 核心指标

| 维度 | 数值 |
|---|---|
| 本地 / npm `latest` | **0.11.2** / **0.11.2**（registry.npmjs.org） |
| 勿用 | **0.11.1**（缺 `loadSessionRules`，已废弃） |
| 架构门（本轮会话） | Risk **MEDIUM**（符号搬迁到 `llm-client` 触发 removed-type / broad-impact，非分层串门） |
| 开源任务（todo/doing） | doing 3 · 工程轨 W16+ / 行为契约 W19–W22 仍待开 |
| Git | `main` **ahead 1** vs `origin/main`；工作区大量未提交改动（审核修复 + 其它 WIP） |

## 阶段口径

| 阶段 | 对外说法 | 说明 |
|---|---|---|
| P0 会话门 / 合规 | **可用** | worktree 防护、stale 清理、版本回显、Python from-import、0.11.2 已发布 |
| P1 获客 | 主路径落地；扩展暂缓 | Marketplace / PH 未做 |
| P2 变现 | 能力有；钱未到 | 首笔 Pro / Team 意向仍进行中 |
| 审核补强 | **本轮落地一截** | 配置化 + AI 容错 + 错误分级；增量子图 / 压测未做 |
| P3 壁垒 | 设计有，工程未开 | W19–W22 行为契约 + llm-review |

## 进行中（商业里程碑，未变）

| 任务 | 进度 | 卡点 |
|---|---|---|
| W09-03 首笔 Pro | ~20% | 真实到账 + `pm/metrics.md` 订单号 |
| W12-01 Team 私有化 | ~70% | 书面意向入 `pm/pilots.md` |
| W08-03 真实仓周更 | ~50% | Week 2 复检 |

---

## 下一步清单（已更新 · 2026-09-09）

优先级从上到下。工时是量级，不是承诺。

### A. 今天收尾（工程债，约 1–2h）

1. **审视并提交本轮审核修复**（与其它 WIP 拆开）：`llm-client` / analyzers / risk 配置 / report migrate / scan allow / 对应测试。未要求则不 push、不发 npm。
2. 打开 `.av/session-report.html`，确认黄灯来自符号搬迁；接受后刷新基线：`npm run arch:baseline`（或等价 session start）。
3. 工作区**其它**未提交改动（orch / tutorial-demo / workflows…）单独分堆，不要和审核修复混成一个 commit。
4. 本机若仍用旧 MCP schema，**重启 Cursor MCP** 后再验 `repo` 可选等行为。

### B. 本周工程（产品债 · 选自审核「刻意延后」+ 开源轨）

| 顺序 | 项 | 优先级 | 说明 |
|---|---|---|---|
| 1 | W16-01 小范围分发：3 个真实仓试点 | P0 | 用 **0.11.2** + `--registry https://registry.npmjs.org`；国内镜像可能滞后 |
| 2 | W16-02 退出码契约收口验收 | P1 | HEAD 已有相关提交，需对照验收命令勾完再 `wbs set done` |
| 3 | 大仓性能基线（审核遗留） | P1 | 千级文件上记 `buildGraph` / `diffGraphs` 耗时与内存；**不做**假增量全图 |
| 4 | 多场景 Golden（跨层 / 环 / 删类型） | P2 | 补 2–3 份典型 finding 金文件 |
| 5 | 文档：`architecture-rules` 的 `risk:` 段 + `AV_SCAN_ALLOW` / 错误分级 | P2 | 写入 quickstart 或 SESSION-GUIDE |

### C. 商业 / 分发（只有维护者能推进）

1. 首笔 Pro 真实付款并记台账（W09-03 / W17-01）。
2. Team / On-prem 书面意向（W12-01 / W18-01）。
3. 指标台账周五刷 npm 周下载（`node pm/scripts/metrics-pull.mjs`）。
4. 扩展 Marketplace（W06-01）仍 **暂缓**，除非改口。

### D. 下一阶段工程（开新轨才做）

| 轨 | 任务 | 说明 |
|---|---|---|
| 行为契约 | W19 → W20 → W21 | call 边 / 签名 / 行为指纹 / 语义规则族 |
| AI 评审 | W22 | llm-review；复用本轮 `llm-client` 容错，勿再复制一套 fetch |
| 云端聚合 | W13-* | OAuth / 多仓门户；**不是** MCP `av_batch_scan` |

### E. 明确不做（除非你改口）

- 不为「增量」牺牲环检测：builtin 不全量子图重算。
- 不为规则引擎加拓扑排序。
- 不新增 MCP 批量扫仓工具；多仓走 CLI / `lib/workspace.js`。
- 不从脏工作区 `npm publish`；发版只从干净 tag/clone。
- 不自动 commit / push / 刷新基线。

---

## 建议

1. **先把审核修复单独 commit**，再决定是否进下一版 patch（0.11.3）；0.11.2 已可用，不必为文档级改动立刻重发。
2. **试点用 0.11.2**，验证 session 门 + 新规则配置，而不是继续堆本地 WIP。
3. **变现与试点并行**：工程债清一点就够；首笔 Pro / Team 意向仍是卡收入的主瓶颈。

## 相关文档

- npm 发布 SOP：[docs/npm-publish.md](../../docs/npm-publish.md)
- 规则示例（含 `risk:`）：[architecture-rules.example.yaml](../../architecture-rules.example.yaml)
- 指标台账：[pm/metrics.md](../metrics.md)
- 上一份快照：[STATUS-2026-09-05.md](./STATUS-2026-09-05.md)
