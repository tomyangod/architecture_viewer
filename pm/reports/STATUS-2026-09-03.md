# 项目现状归档 · 2026-09-03

> 性质：会话归档 + 代码/WBS 快照。数据来自 git、`npm test`、`arch-viewer session report`、W02 任务卡逐条核实。
> 快照日：周四 2026-09-03。上一份发版：0.10.0（2026-09-02）。工作区构成 0.11.0 候选，未提交、未打 tag。

## 一、全部对话脉络（产品主线）

这条对话不是一次修 bug，而是把产品从「C4 六视图生成器」拧成「AI 改完代码后看结构变了什么」。两条路线并行：

- **AI 可见性（本对话主线）**：提取 → 会话 diff → 风险/影响面 → 分发与 Agent 接入。
- **商业化 WBS（`pm/`）**：Phase 0–3 / W01–W13，与主线工程进度不同步。

### 阶段 A — 提取与 diff（已完成）

tree-sitter 提取器覆盖 Java / JS-TS / Vue / Svelte / Python / Go。`extract` / `diff` 产出图谱与结构差。本仓 dogfood（忽略 test/eval/examples/docs 后）产品 `lib/` 召回接近满，噪声接近 0。正式「三仓 ≥80% 对人」闸门当初跳过，后用本仓回归补上。

### 阶段 B — 会话体验（已完成，仍在打磨）

`session start` 写 `.av/graph-baseline.json`；`session report` 出文本 + JSON + 自包含 HTML。高风险 exit 1。后续补齐：gitignore 感知扫描、DEV_SKIP_DIRS、扁平 `addedEdges`、重命名配对、默认只看变更、`--all` 看归属边。HTML 对比图是本会话最后一次打磨点（见第二节）。

习惯门：工作日 18:30 提醒至约 2026-09-16，日志 `.av/habit-gate-log.md`（gitignore）。

### 阶段 C — 计划是分发，实际是工程能力

计划：Marketplace、PR 评论、落地页、500 安装 / 300 star / 20 反馈。

实际交付：TRAE 规则、多仓 workspace、影响面 BFS、风险与影响面挂钩、扩展会话模式（状态栏 + watcher + webview）。获客数字未达；工程侧已可用。

### T1–T4 / 0.8.0 — 分发前置（已发版）

icon、`build-vsix.js`（临时摘 `files` 再恢复）、PR 评论 + GitHub Action、新落地页、README 重写、反馈表、小白演示仓。

### 0.9.0 — 分层推断（已发版）

四信号分层（配置 / import 语义 / 目录约定 / 结构兜底）。`.av/layers.suggested.json` 自动建议。真实舆情仓分层覆盖约 57% → 92%。`database/` 不再因正则 `/db/` 漏判。

### 0.10.0 — MCP Server（已发版）

stdio JSON-RPC，工具：`av_session_start` / `av_session_report` / `av_check_layering` / `av_explain_finding`。不暴露图谱查询。教程：`docs/SESSION-GUIDE.md`。演示：`docs/MCP-DEMO-publicopinionmonitor.md`。

### 舆情仓 dogfood（`publicopinionmonitor_v2`）

约 246 个 py/js 文件。旧 0.8.x 能看见新 import，但 `database/` 无层则无 HIGH；0.9+ 能打出跨层 HIGH。全仓 `av_check_layering` 约 218 条 HIGH（历史 `tools→service` 债），**不能当日常会话门**——日常只用 `av_session_report` 看本轮。用户 Cursor `mcp.json` 当时只有 chart；写入 arch-viewer 与 POM `AGENTS.md` 未获确认，未改对方仓库。

### 本会话最后一次工程改动

`lib/session-report.js` Before/After：按文件容器分组；纯新增时 Before 不再空白（changed 过滤器含变更实体所在文件的全部成员）；左右共用布局；边高亮认起点和终点。

---

## 二、代码现状

### 版本与质量

| 项 | 值 |
|---|---|
| `package.json` | 0.10.0 |
| git | `main` 超 `origin/main` 24 个 commit；工作区另有未提交变更 |
| 测试（2026-09-03） | 234 用例，233 通过，0 失败，1 跳过 |
| 架构门 | 退出码 0，Risk NONE（0 findings） |
| 相对基线结构差 | 节点 +6 -0 ~1，边 +6；全部在 `mcp/server.js` watcher |

基线未刷新。门规：人工确认后再 `npm run arch:baseline`。

### 未提交变更（相对 0.10.0，约 +1800 行，0.11.0 候选）

| 特性 | 文件 | 内容 |
|---|---|---|
| MCP 自动闭环 | `mcp/server.js`、`test/mcp.test.js` | 静默约 20s 自动 diff 并缓存；`av_session_changes` 轻量轮询；start 时拉起 watcher |
| 一键接入 | `lib/setup.js`、`test/setup.test.js`、`lib/cli.js` | `arch-viewer setup` 写 Cursor / Claude Code / Claude Desktop / Windsurf / DeepSeek Harness；备份、可重复、优先 npx |
| 对比图 | `lib/session-report.js`、`lib/diff-graph.js` | 文件容器布局与过滤器 |
| 文档 | README、SESSION-GUIDE、MCP-DEMO、welcome.html、dsh 示例、CHANGELOG | setup 与 MCP 教程 |

`CHANGELOG.md` 仍停在 0.10.0，**0.11.0 条目未写**。`.av/graph-baseline.json` 工作区已改但未提交，且仍落后于 watcher 六函数。

架构报告只能看见最后一个特性（+6 函数）。setup 与对比图的结构变化已溶进当前基线或未被扫描器当成新类型，会话门对「三个特性堆在一起」已经失真。

---

## 三、商业化进度（WBS 核实）

全局：46 任务，W01 完成 6/6。今天是 **W02 周期（09-07 ~ 09-11）开始前 4 天**。任务卡显示 W02 0/6，自动化验收与卡面不一致：

| 任务 | 卡面 | 核实 |
|---|---|---|
| W02-01 vsix 脚手架 | 0% | `scripts/build-vsix.js` 在，package.json 的 icon/repository/engines/categories 齐全；待实跑 `vsce package` 留证 |
| W02-02 网页离线 zip | 0% | `scripts/build-web-dist.mjs` 不存在，未做 |
| W02-03 Webview CSP | 0% | `src/extension.js` 无 cdn.jsdelivr；断网 F5 人工项待做 |
| W02-04 去 Python | 0% | `scripts/demo.sh` 无 python；README 主路径人工确认待做 |
| W02-05 冒烟清单 | 0% | `pm/checklists/` 不存在，未做 |
| W02-06 邮箱账号骨架 | 0% | `account/` 不存在；web 侧 `web/lib/pro/auth.js` 已有实现 |

`docs/feedback-tracking.md` W1（09-03 ~ 09-09）渠道指标为空。未 push、未上 Marketplace，表空是预期。

未改 `pm/tasks/tasks.json`：卡面校准需要你确认人工验收项后再 `wbs set`。

---

## 四、下一步清单（已更新）

优先级从上到下。工时是量级，不是承诺。

### A. 今天收尾（约 1h，不改 WBS）

1. 打开 `.av/session-report.html`，看 Before/After 文件容器是否符合预期。
2. 确认后刷新基线：`npm run arch:baseline`。
3. 补写 CHANGELOG 0.11.0（watcher 防抖、setup 支持的工具、过滤器行为）。
4. 工作区拆三个 commit：`feat(mcp) watcher`、`feat(cli) setup`、`fix(report) 对比图`；文档跟对应 commit。未要求则不 push、不打 tag。
5. 决定 0.11.0 是否立刻发（建议立刻发，理由见第五节）。

### B. 本周可选（W02 正式开始前可预做）

| 顺序 | 项 | 工时 | 说明 |
|---|---|---|---|
| 1 | 校准 W02-01/03/04 | 1h | 实跑 vsce、断网 F5、通读 README，再 `wbs set … done` |
| 2 | W02-06 账号骨架 | 6h | 本周最大块、P0；复用 `web/lib/pro/auth.js` 协议，不要另起炉灶 |
| 3 | W02-02 网页 zip | 4h | 无阻塞 |
| 4 | W02-05 冒烟清单 | 2h | 走查 01/03 时顺手写 `pm/checklists/release-smoke.md` |

### C. 对话里未关闭、不阻塞发版

- 用户确认后：写本机 `~/.cursor/mcp.json` 的 arch-viewer 项；舆情仓 `AGENTS.md` 会话门说明（不改对方业务代码）。
- 习惯门回顾约 2026-09-16。
- 全仓 `av_check_layering` 不当日常闸门；分层规则若还要加，先看 `.av/layers.suggested.json`。
- 反馈表：0.11.0 发出去之后，周日填 W1 渠道数。

### D. 明确不做（除非你改口）

- 不自动刷新基线、不自动 commit/push/tag。
- 不把 218 条历史跨层当成会话失败。
- 不等 W02 全部完成再发 0.11.0。

---

## 五、建议

1. **0.11.0 尽快发，不必等 W02。** watcher 让 Agent 停下即有缓存报告；setup 降低接入成本。反馈表要验证的假设（有人真用会话门、有人接 PR 评论）都靠装机量。

2. **一个特性一提交一刷基线。** 三个特性堆在工作区后，门里只剩 +6 函数。setup 与对比图已经无法从当前报告复盘。

3. **WBS 卡面今天或 W02 周一校准。** 三张卡自动化项已过仍显示 0%，周五周报会假空。未跑人工项前不要标 done。

4. **账号体系扩现有 web auth，不要新协议。** 任务卡路径（`account/auth-client.js` + `web/lib/auth.js`）与此一致。

5. **CHANGELOG 跟 commit 写。** watcher 防抖秒数、setup 工具名单、过滤器语义现在还在代码和会话里，隔几天会丢。

6. **MCP 工具分工对 Agent 说清楚。** `av_session_report` = 本轮；`av_check_layering` = 全仓债。教程已写，接入配置和 AGENTS.md 还没落到用户仓。
