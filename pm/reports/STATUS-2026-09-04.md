# 项目现状归档 · 2026-09-04

> 性质：会话收口 + 下一步清单。数据：git、`package.json` 0.11.0、`session report`（相对当前基线 +0 −0 ~0）。
> 上一份归档：`pm/reports/STATUS-2026-09-03.md`。上一份发版：0.11.0（`80f0652`）。

## 一、总结（到今天为止）

### 产品

Architecture Viewer 已定位为 **AI 编码架构验收门**（不是画图工具）。主路径：提取 → 会话基线 → diff / 风险 / 影响面 → CLI / 扩展 / MCP。图是报告呈现。

已发版到 **0.11.0**：MCP watcher 自动闭环、`arch-viewer setup`、Before/After 对比图修复。`main` 超 `origin/main` **31** 个 commit。CHANGELOG 已有 0.11.0 条目。

### 本轮（9/4）未提交、已落基线的工程

Archify 作渲染层（分析仍是我们的）：

| 块 | 状态 |
|---|---|
| Spike + 硬约束（id / grid / clean-flow / 无 sources） | 完成 |
| `lib/export-archify.js` IR 适配器 | 完成 |
| `lib/archify-export.js` 编排 + CLI `archify-export` + MCP `av_archify_export` | 完成 |
| `session report --renderer auto`：validate+compare 成功用 Archify HTML，失败回退内置 | 完成，本仓实测通 |
| 架构基线 | 已刷，当前门 **+0 −0 ~0，NONE** |
| `archify-main/` 参考副本 | 工作区未跟踪；扫描已跳过该目录名 |
| CHANGELOG / 正式 vendor 入库 | **未做**（这批应视为 0.12.0 候选） |

商业文档：`COMMERCIAL-v2.md` 已写（学分发不学赞助当主业；分析免费；不整仓改 GPL）。尚未回写 `COMMERCIAL.md` / README。

### WBS

W01 完成。W02 校准后 **3/6**：02 网页 zip、04 去 Python、05 冒烟清单已 done。**01 vsix / 03 Webview CSP 标 deferred**（先不做 VS Code 扩展）。06 账号骨架仍 todo（CLI/网页，扩展端暂缓）。

---

## 二、下一步清单（已更新）

### A. 立刻（发版收口，约 1–2h）

1. **拆 commit 提交 Archify 集成**（未要求则不 push）：建议  
   - `feat(archify): IR 导出适配器`（`lib/export-archify.js` + 单测）  
   - `feat(cli): archify-export + MCP av_archify_export`  
   - `feat(report): --renderer auto 回退内置`  
   - `docs: 集成方案 + 商业 v2 + 本归档`  
   - **不要**把整个 `archify-main/` 打进产品 commit（体积大；产品路径应是日后 `vendor/archify/` 钉版本）。
2. **补 CHANGELOG 0.12.0 Unreleased**（导出 / CLI / MCP / `--renderer` / 硬约束与回退）。
3. `.av/archify-*.json` 是会话产物，确认已 gitignore；基线 `.av/graph-baseline.json` 随 chore 提交。

### B. 本周预做 / W02（09-07 起，截止 09-11）

| 顺序 | 项 | 说明 |
|---|---|---|
| 1 | W02-06 账号骨架 | 本周最大块（约 6h），CLI/网页接入，复用 `web/lib/pro/auth.js`；扩展端不做 |
| 2 | README 首句改「架构验收门」 | 商业 v2 的 P0，约 3h，不改 LICENSE；不写 Marketplace 安装为主路径 |
| 3 | README 赞助位（1 个真实在用工具）+ Sponsor 按钮 | 学 Archify 货架，约 1.5h |

### C. 随后（W03–W04，不挡发 0.12）

- Phase 2 余量：`scripts/vendor-archify.mjs` 钉 tag 写入 `vendor/archify/`（替代工作区里的 `archify-main/`）；默认 renderer 先保持 **auto + 失败回退**，观察一周再谈「只链精美视图」。
- Phase 0 欠账：`diff-graph` 的 moved/rerouted（我们语义）、session-report golden（2–3 fixture）。
- Phase 3：`archify-trace` skill、上游 issue——等 0.12 提交且真实仓 compare 稳定后再写。
- `COMMERCIAL.md` 用 v2 第三、四节替换过时六视图卖点。

### D. 明确不做

- 不自动 push / 打 tag / 改整仓 GPL。
- 不把全仓 `av_check_layering` 当日常门。
- 不把增量扫描做成 Pro 付费墙。

---

## 三、意见建议

1. **先提交 0.12.0 候选，再开 W02-06。** 集成代码已过门、已刷基线，再堆账号骨架会再次稀释会话门信号。拆 3–4 个 commit 比一锅端清楚。
2. **`archify-main/` 不要当正式 vendor。** 继续当本地参考即可；入库用钉版本的 `vendor/archify/` + LICENSE/NOTICES。现在进 git 会拖累 clone。
3. **默认 renderer 维持 auto。** 本仓有 vendor 时已走出 Archify 主 HTML；无 CLI 的用户自动内置。计划第八节「长期 builtin 为主」可让位于「auto + 永久 fallback」，不必再改回默认 builtin。
4. **先不做 VS Code 扩展。** W02-01/03、W03-03、W06-01 已 deferred；冒烟清单扩展段不挡发版。本周只走 CLI / MCP / 网页。
5. **获客文案优先于合作 issue。** 定位句和赞助位半天能做；Archify 互推没有已提交的导出命令当证据，空口没有用。

需要我按 A 起草 0.12.0 changelog 或按你的确认拆 commit 时再说一声。
