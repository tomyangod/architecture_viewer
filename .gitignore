# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
.idea/
.vscode/*
!.vscode/launch.json
*.code-workspace

# Env & secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Local eval / temp
_*.py
_*.json
*.log
.tmp/
temp/
dist/
build/
eval/out/
.data/

# Playwright / test artifacts
test-results/
playwright-report/
.arch_*.json
.arch_*.png

# Trae share packages (large zips)
.trae-html-share-packages/

# Local harness / agent dumps (not product)
deepseek-harness/

# Archify reference copy (local spike only, not product code)
archify-main/
.archify-*/

# Architecture Viewer session artifacts (local only)
# With git, session report defaults to HEAD; do not commit runtime baselines.
# Keep .av/layers.json tracked when teams intentionally share layer config.
.av/graph-baseline.json
.av/session-report.*
.av/session-history.jsonl
.av/session-intent.json
.av/archify-*.json
.av/habit-gate-log.md
.av/workspace.json
.av/layers.suggested.json
.av/graph-head.json
.av/validation-report.json
.av/awareness-cursor.json

*.vsix
*.tgz
